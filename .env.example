# OpenRouter API key — required for AI features (future tickets)
OPENROUTER_API_KEY=

# JWT signing secret — change this to a strong random string in production
JWT_SECRET=change-me-to-a-strong-random-secret
